distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
print(r_squared)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "EPA")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
view(filtered_for_stats_df)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "EPA")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
view(filtered_for_stats_df)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "EPA")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
view(filtered_for_stats_df)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
subset(select=-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
view(filtered_for_stats_df)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
view(filtered_for_stats_df)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
print(r_squared)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#r_squared = coef(lm(sn~epa_sn_str, data = filtered_for_stats_df))
}
print(epa_sn_str)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
?lm
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#first ensure that the X and Y variables in the formular are properly stated
formatted_formular = reformulate(termlabels = sn, response = epa_sn_str)
r_squared = coef(lm(formatted_formular, data = filtered_for_stats_df))
}
print(epa_sn_str)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn)
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#first ensure that the X and Y variables in the formular are properly stated
formatted_formular = reformulate(termlabels = sn, response = epa_sn_str)
#r_squared = coef(lm(formatted_formular, data = filtered_for_stats_df))
}
print(formatted_formular)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn[1])
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#first ensure that the X and Y variables in the formular are properly stated
formatted_formular = reformulate(termlabels = sn, response = epa_sn_str)
#r_squared = coef(lm(formatted_formular, data = filtered_for_stats_df))
}
print(formatted_formular)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
?reformulate
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn[1])
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#first ensure that the X and Y variables in the formular are properly stated
formatted_formular = reformulate(termlabels = sn, response = epa_sn_str)
r_squared = coef(lm(formatted_formular, data = filtered_for_stats_df))
}
print(r_squared)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn[1])
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#first ensure that the X and Y variables in the formular are properly stated
formatted_formular = reformulate(termlabels = (sn), response = (epa_sn_str))
r_squared = coef(lm(formatted_formular, data = filtered_for_stats_df))
}
print(r_squared)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn[1])
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#first ensure that the X and Y variables in the formular are properly stated
formatted_formular = paste0("`",epa_sn_str,"`~`",sn,"`")
r_squared = coef(lm(as.formula(formatted_formular),
data = filtered_for_stats_df))
}
print(r_squared)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
find_r2_rmse = function(location_str,param_str){
#Get the devices at this location for
#which the r-squared values are to be determined
#Execlude the epa devices since they will determined separately
devices_sn_df = correction_factor_sensors_data |>
filter(site_name==location_str,
manufacturer!="EPA") |>
subset(select = c("device_sn")) |>
distinct(device_sn)
#Make the device sn list vector to allow looping through the devices
devices_sn_vec = as.vector(devices_sn_df$device_sn[1])
#Filter for data at only this location and for only these parameters
filtered_for_stats_df = correction_factor_sensors_data |>
filter(site_name==location_str,
parameter==param_str) |>
#Remove the manufacturer column to prevents duplicates in pivoting
select(-c("manufacturer")) |>
#Widen the data so as to be able to create x
#and y coordinates in fitting the regression models
pivot_wider(names_from = c(device_sn),
values_from = pm_gases_measures)
#Instantiate lists to collect the r-squared and rmse values
r_squared_vec = rep(0, length(devices_sn_vec))
rmse_vec = rep(0, length(devices_sn_vec))
#Calculate and collect the r-squared and rmse for each device
for (sn in devices_sn_vec){
#Obtain the EPA serial number to be used in finding r-squared
epa_sn_str = epa_sn_df[epa_sn_df$site_name==location_str, "device_sn"][1]
#Find the r-squared for each device
#first ensure that the X and Y variables in the formular are properly stated
formatted_formular = paste0("`",epa_sn_str,"`~`",sn,"`")
r_squared = summary(lm(as.formula(formatted_formular),
data = filtered_for_stats_df))
}
print(r_squared)
}
find_r2_rmse(location_str = "Harrisburg", param_str = "PM2.5")
